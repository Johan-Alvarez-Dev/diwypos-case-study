openapi: 3.1.0
info:
  title: DiwyPOS Public API Contract
  version: 1.0.0
paths:
  /api/public/menu:
    get:
      summary: Return a sanitized public menu
      responses:
        '200': { description: Available categories and menu items }
